#include <stdio.h>

int main()
{
    int x = 100;
    int y = 0;

    while (x > y)
    {
        printf("y is: %d\n", y);
        y++;
    }

    // for (int i = 0; i < x; i++)
    // {
    //     y = i;
    //     printf("y is: %d\n", y);
    // }

    // char answer[] = get_line("What's your name?\n");
    // printf("%s", answer);

    // declaring string
    // char answer[50];

    // reading string
    // printf("What is your name?: ");
    // scanf("%s", answer);
    // printf("Your name is %s\n", answer);

    // print string

    return 0;
}