#include <stdio.h>
/* This is a comment */

int main()
{

    int x = 1;
    int y = 1;

    if (x < y)
    {
        printf("X is less than y\n");
    }
    else if (x == y)
    {
        printf("x and y are the same\n");
    }
    else
    {
        printf("Y is less than x\n");
    }

    return 0;
}